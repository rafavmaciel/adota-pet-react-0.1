version: "3"

services:
    reactjs:
        container_name: adota_pet
        build: .
        user: '1000'
        volumes:
          - '.:/usr/src/app'
          - '/usr/src/app/node_modules'
        ports:
          - '3000:3000'
        command: npm start
